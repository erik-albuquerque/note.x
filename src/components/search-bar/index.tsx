import { SearchBarIcon } from './search-bar-icon'
import { SearchBarInput } from './search-bar-input'
import { SearchBarRoot } from './search-bar-root'
import { SearchBarShortcut } from './search-bar-shortcut'

const SearchBar = {
  Root: SearchBarRoot,
  Input: SearchBarInput,
  Shortcut: SearchBarShortcut,
  Icon: SearchBarIcon
}

export { SearchBar }
