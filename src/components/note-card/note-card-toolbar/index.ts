import { NoteCardToolbarAction } from './note-card-toolbar-action'
import { NoteCardToolbarRoot } from './note-card-toolbar-root'

const NoteCardToolbar = {
  Root: NoteCardToolbarRoot,
  Action: NoteCardToolbarAction
}

export { NoteCardToolbar }
