import { ToolbarButton as Button } from './toolbar-button'
import { ToolbarContent as Content } from './toolbar-content'
import { ToolbarRoot as Root } from './toolbar-root'
import { ToolbarView as View } from './toolbar-view'

const Toolbar = {
  Root,
  Content,
  Button,
  View
}

export { Toolbar }
